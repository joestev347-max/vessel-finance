# Keep binary formats out of line-ending normalization (PDFs generated by
# reportlab lack NUL bytes early in the file, so git misclassifies them as text)
*.pdf binary
*.png binary
*.jpg binary
*.zip binary
*.skill binary
*.db binary
